{% extends 'base.html.twig' %}

{% block title %}Ville
{% endblock %}

{% block body %}
	<h1>Ville</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ ville.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ ville.name }}</td>
			</tr>
			<tr>
				<th>CodePostal</th>
				<td>{{ ville.codePostal }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_ville_index') }}">Retour</a>

	<a href="{{ path('app_ville_edit', {'id': ville.id}) }}">Modifier</a>

	{{ include('ville/_delete_form.html.twig') }}
{% endblock %}
